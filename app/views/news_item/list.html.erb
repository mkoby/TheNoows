<div style="margin-top: 25px; margin-left: 200px; margin-right: 200px;">
  <% if @items.empty? %>
    <h2>No News Items to Display *sad panda*</h2>
  <% else %>
    <% @items.each do |item| %>
      <div class="newsitem">
        <p>
        <span>
          <div class="votebutton" style="float: left; padding-right: 25px;">
            <a href="news_items/<%= item.id %>/vote_up">Vote Up</a>
          </div>
          <div class="iteminfo" style="padding-left: 50px;">
            <div class="itemlink" style="font-size: 26px;">
              <a href="<%= item.link %>"><%= item.title %></a>
            </div>
            <div class="undertitle"> 
              published at <%= item.published_at %> on <a href="<%= item.source.homepage_url %>"><%= item.source.name %></a>
            </div>
          </div>
        </span>
        </p>
        </br>
      </div>
    <% end %>
  <% end %>
</div>
